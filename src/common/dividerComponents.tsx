import styled from '@emotion/styled';

export const BoldColumnDivider = styled.div({
  borderTop: '2px solid',
});

export const NormalColumnDivider = styled.div({
  borderTop: '1px solid #DDDDDD',
});

export const BoldRowDivider = styled.div({
  borderLeft: '2px solid',
});

export const NormalRowDivider = styled.div({
  borderLeft: '1px solid #DDDDDD',
});
